# Security

The lint reads handoff files and reports on them; it does not execute them. If you find a way for a handoff to make
the lint or the tests execute code, write outside a temporary directory, or expose secrets, please report it through
this repository's private vulnerability reporting rather than a public issue.
